providers:
  - id: gemini:gemini-2.0-flash-exp-image-generation
    config:
      generationConfig:
        responseModalities: ['Text', 'Image']

prompts:
  - Generate an image of a dog on a skateboard. Then describe the image in detail.
